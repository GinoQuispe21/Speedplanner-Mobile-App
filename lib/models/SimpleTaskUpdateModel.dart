class SimpleTaskUpdateModel {
  int id;
  bool finished;
  String deadline;
  String title;
  String description;
  SimpleTaskUpdateModel(
      this.id, this.finished, this.deadline, this.title, this.description);
}
